# Copy to .env and generate unique keys before starting the stack.
# Generate admin keys: openssl rand -hex 32

APISIX_IMAGE_TAG=3.17.0-debian
APISIX_ADMIN_KEY=<Admin API KEY>
APISIX_VIEWER_KEY=<VIEWER KEY>
